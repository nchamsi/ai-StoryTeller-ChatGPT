<!DOCTYPE html>
{# data-theme "light" ou "dark" pour le thème de l'interface de base #}
<html data-theme="dark">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}🧞‍♂️ Raconte-moi mon HISTOIRE! 👩‍🎤 🧌{% endblock %}</title>
        {# rajout icone pour onglets de navigateur #}
        <link rel="icon" href="{{ asset('images/story.png') }}">
        {# ajout CSS custom si necéssaire #}
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {# script HTMX #}
        <script src="https://unpkg.com/htmx.org@1.8.4" integrity="sha384-wg5Y/JwF7VxGk4zLsJEcAojRtlVp1FKKdGy1qN+OMtdq72WRvX/EdRdqg/LOhYeV" crossorigin="anonymous"></script>
        {# TailwindCSS #}
        {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body class="background">
        <main class="container my-20 px-6 mx-auto mb-40">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
